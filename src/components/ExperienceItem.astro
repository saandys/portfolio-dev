---

export interface Tag {
    name: string;
    class: string;
    icon: any;
}
export interface Props{
    title: string 
    description: string 
    link: string
    date: string
    tags: Tag[]
}
const {title, description, link, date, tags} = Astro.props
---

<div class="absolute w-3 h-3 bg-pink-500 rounded-full mt-1.5 -start-1.5 border border-pink-500 "></div>
<time 
    class="mb-1 text-sm font-normal leading-none text-gray-500"
    >{date}</time>
<h3     
    class="text-lg font-semibold text-pink-500 mt-4 text-pink-500"
    >{title}</h3>
<p 
    class="mb-4 text-base font-normal text-gray-200 text-pretty text-gray-400"
    >{description}</p>
    <ul class="flex gap-x-4 flex-row flex-wrap w-full gap-y-3">
        { tags.map((tag) => (
             <li>
                  <span class={`flex gap-x-2 rounded-full text-xs ${tag.class} py-1 px-2`}>
                     <tag.icon class="size-4"/>
                     {tag.name}
                 </span>
                
             </li>
         )) }
     </ul>

    {
        link && (
            <a href={link} 
                class="inline-flex items-center px-4 py-2 text-sm font-medium border  rounded-lg  focus:z-10 focus:ring-4 focus:outline-none  focus:text-blue-700 bg-gray-800 text-gray-400 border-gray-600 hover:text-white hover:bg-gray-700 focus:ring-gray-700"
                >Learn more 
                <svg 
                    class="w-3 h-3 ms-2 rtl:rotate-180" 
                    aria-hidden="true"
                    xmlns="http://www.w3.org/2000/svg" 
                    fill="none" 
                    viewBox="0 0 14 10"
                >
                <path 
                    stroke="currentColor" 
                    stroke-linecap="round" 
                    stroke-linejoin="round" 
                    stroke-width="2" 
                    d="M1 5h12m0 0L9 1m4 4L9 9"/>
        </svg></a>
        )
    }
        
    